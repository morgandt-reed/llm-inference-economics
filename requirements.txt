# Runtime dependencies. Two, both pure Python. The model layer itself has none:
# it is arithmetic over frozen dataclasses. Click is the CLI, PyYAML reads the
# price file. Anything heavier would be a dependency on the presentation layer
# leaking into a calculation anyone should be able to audit by reading it.
click>=8.1
PyYAML>=6.0

# Development and CI.
pytest>=8.0
pytest-cov>=5.0
ruff==0.6.9
